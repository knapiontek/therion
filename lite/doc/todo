Separate range from iterator
Index can convert to lambda
Find by lambda
core::zip

enum -> enum class
use moder casting static_cast<> vs ()

erase_by_iterator -> erase_by_collection
release mode replaces const char* with static String
build statistics for list/queue/map/set page size
test const char* for string append, prepend, arg, format etc


add full support for int128 and float128 when supported
do {} while check consistency
lang::reader more testing

bugs:
3f32 is recognized as int64
