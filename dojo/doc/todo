call closed loop detection
import other files
syntax error does not stop execution (SYNC does not work)

=========================================================================
=========================================================================
